@model IList<MvcApplication16.Models.TestModel>
@{
    ViewBag.Title = "Cabinet";
}

<h2>Cabinet</h2>
@ViewBag.message

        @foreach (var i in Model)
        {
            @Html.Partial("_ShowTLink",i)
        }